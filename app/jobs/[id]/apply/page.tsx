'use client';import{useState}from'react';import{createClient}from'@/lib/supabase/client';export default function Apply({params}:{params:{id:string}}){const[name,setName]=useState(''),[mobile,setMobile]=useState(''),[email,setEmail]=useState(''),[msg,setMsg]=useState('');return <main className="container-gws max-w-2xl py-16"><div className="card p-7"><h1 className="text-3xl font-bold text-navy">চাকরির আবেদন</h1><form className="mt-7 space-y-5" onSubmit={async e=>{e.preventDefault();const s=createClient();const c=await s.from('candidates').insert({full_name:name,mobile,email,date_of_birth:'2000-01-01',address:'পরে যাচাই',district:'—',state:'—',passport_status:'no',education:'পরে যাচাই',skills:[],status:'new'}).select('id').single();if(c.error){setMsg('আবেদন জমা দেওয়া যায়নি।');return}const a=await s.from('applications').insert({job_id:params.id,candidate_id:c.data.id,status:'pending'});setMsg(a.error?'আবেদন জমা দেওয়া যায়নি।':'আপনার আবেদন সফলভাবে জমা হয়েছে।')} }><div><label className="label">পূর্ণ নাম</label><input required className="input" value={name} onChange={e=>setName(e.target.value)}/></div><div><label className="label">মোবাইল</label><input required className="input" value={mobile} onChange={e=>setMobile(e.target.value)}/></div><div><label className="label">ইমেইল</label><input required type="email" className="input" value={email} onChange={e=>setEmail(e.target.value)}/></div><button className="btn-emerald w-full">আবেদন জমা দিন</button>{msg&&<p className="rounded-xl bg-slate-100 p-4">{msg}</p>}</form></div></main>}
