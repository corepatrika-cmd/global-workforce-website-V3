import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data}=await s.from('faqs').select('*').eq('is_active',true).order('order');return <main className="container-gws max-w-4xl py-16"><h1 className="text-4xl font-bold text-navy">সাধারণ প্রশ্ন</h1><div className="mt-8 space-y-3">{data?.map(f=><details className="card p-5" key={f.id}><summary className="cursor-pointer font-bold">{f.question_bn}</summary><p className="mt-3 leading-7 text-slate-600">{f.answer_bn}</p></details>)}</div></main>}
