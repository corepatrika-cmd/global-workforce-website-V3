import Link from'next/link';import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data}=await s.from('countries').select('*').eq('is_active',true).order('name_bn');return <main className="container-gws py-16"><h1 className="text-4xl font-bold text-navy">দেশসমূহ</h1><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{data?.map(c=><Link href={`/countries/${c.id}`} className="card p-6" key={c.id}><b>{c.name_bn}</b><p className="mt-2 text-sm text-slate-500">{c.description_bn}</p></Link>)}</div></main>}
