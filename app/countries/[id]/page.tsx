import{notFound}from'next/navigation';import{createClient}from'@/lib/supabase/server';import Link from'next/link';export default async function Page({params}:{params:{id:string}}){const s=await createClient();const{data:c}=await s.from('countries').select('*').eq('id',params.id).eq('is_active',true).single();if(!c)notFound();return <main className="container-gws py-16"><Link href="/countries">← দেশসমূহে ফিরুন</Link><div className="card mt-6 p-8"><h1 className="text-4xl font-bold text-navy">{c.name_bn}</h1><p className="mt-4 leading-8 text-slate-600">{c.description_bn}</p></div></main>}
