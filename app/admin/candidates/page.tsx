import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data}=await s.from('candidates').select('*').order('created_at',{ascending:false}).limit(100);return <div><h1 className="text-3xl font-bold text-navy">প্রার্থী ব্যবস্থাপনা</h1><div className="card mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">আইডি</th><th className="p-4">স্ট্যাটাস/অ্যাকশন</th><th className="p-4">তারিখ</th></tr></thead><tbody>{data?.map((r:any)=><tr className="border-t" key={r.id}><td className="p-4">{r.id}</td><td className="p-4">{r.status||r.action||'—'}</td><td className="p-4">{new Date(r.created_at).toLocaleString('bn-IN')}</td></tr>)}</tbody></table></div></div>}
