import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data}=await s.from('faqs').select('*').order('order');return <div><h1 className="text-3xl font-bold text-navy">FAQ ব্যবস্থাপনা</h1><div className="mt-6 space-y-3">{data?.map(f=><div className="card p-5" key={f.id}><b>{f.order}. {f.question_bn}</b><p className="mt-2 text-slate-500">{f.answer_bn}</p></div>)}</div></div>}
