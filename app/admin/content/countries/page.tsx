import{createClient}from'@/lib/supabase/server';export default async function Page(){const s=await createClient();const{data}=await s.from('countries').select('*').order('name_bn');return <div><h1 className="text-3xl font-bold text-navy">দেশ ব্যবস্থাপনা</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{data?.map(c=><div className="card p-5" key={c.id}><b>{c.name_bn}</b><p className="text-sm text-slate-500">{c.name_en} • {c.iso_code}</p></div>)}</div></div>}
