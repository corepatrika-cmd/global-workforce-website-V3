'use client';import{useAdminAuth}from'@/contexts/AdminAuthContext';import{useState}from'react';export default function Login(){const{login}=useAdminAuth(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');return <main className="flex min-h-[80vh] items-center justify-center bg-slate-100 p-5"><form className="card w-full max-w-md p-8" onSubmit={async e=>{e.preventDefault();try{await login(email,password)}catch(x:any){setError(x.message)}}}><h1 className="text-3xl font-bold text-navy">অ্যাডমিন লগইন</h1><label className="label mt-7">ইমেইল</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/><label className="label mt-4">পাসওয়ার্ড</label><input className="input" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="mt-4 text-red-600">{error}</p>}<button className="btn-primary mt-6 w-full">লগইন</button></form></main>}
